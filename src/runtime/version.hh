#ifndef ORO_RUNTIME_VERSION_H
#define ORO_RUNTIME_VERSION_H

#include "string.hh"
#include "config.hh"

namespace oro::runtime::version {
  extern const String VERSION_FULL_STRING;
  extern const String VERSION_HASH_STRING;
  extern const String VERSION_STRING;
}
#endif
