.TH ORO-SHARED-WORKER-INIT 3 "" "Oro Runtime" "Oro Runtime API Manual"
.SH NAME
oro:shared\-worker/init \- JavaScript API module
.SH SYNOPSIS
.nf
import * as module from 'oro:shared\-worker/init'
.fi
.SH DESCRIPTION
.PP
oro:shared\-worker/init is a JavaScript API module exposed by the Oro Runtime.
.SH REFERENCE
.TP
\fBworkers\fR
This is an exported `VariableDeclaration` named `workers`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBSharedWorkerInstance (extends Worker)\fR
This is an exported `ClassDeclaration` named ``SharedWorkerInstance` (extends `Worker`)`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBSharedWorkerInfo\fR
This is an exported `ClassDeclaration` named `SharedWorkerInfo`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonInstall(event)\fR
This is an exported `FunctionDeclaration` named `onInstall`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonUninstall(event)\fR
This is an exported `FunctionDeclaration` named `onUninstall`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonConnect(event)\fR
This is an exported `FunctionDeclaration` named `onConnect`, but it does not yet have a dedicated description in the generated reference.
