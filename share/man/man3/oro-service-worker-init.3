.TH ORO-SERVICE-WORKER-INIT 3 "" "Oro Runtime" "Oro Runtime API Manual"
.SH NAME
oro:service\-worker/init \- JavaScript API module
.SH SYNOPSIS
.nf
import * as module from 'oro:service\-worker/init'
.fi
.SH DESCRIPTION
.PP
oro:service\-worker/init is a JavaScript API module exposed by the Oro Runtime.
.SH REFERENCE
.TP
\fBworkers\fR
This is an exported `VariableDeclaration` named `workers`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBchannel\fR
This is an exported `VariableDeclaration` named `channel`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBServiceWorkerInstance (extends Worker)\fR
This is an exported `ClassDeclaration` named ``ServiceWorkerInstance` (extends `Worker`)`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBServiceWorkerInfo\fR
This is an exported `ClassDeclaration` named `ServiceWorkerInfo`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonRegister(event)\fR
This is an exported `FunctionDeclaration` named `onRegister`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonUnregister(event)\fR
This is an exported `FunctionDeclaration` named `onUnregister`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonSkipWaiting(event)\fR
This is an exported `FunctionDeclaration` named `onSkipWaiting`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonActivate(event)\fR
This is an exported `FunctionDeclaration` named `onActivate`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonFetch(event)\fR
This is an exported `FunctionDeclaration` named `onFetch`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonNotificationShow(event, target)\fR
This is an exported `FunctionDeclaration` named `onNotificationShow`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonNotificationClose(event)\fR
This is an exported `FunctionDeclaration` named `onNotificationClose`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonGetNotifications(event)\fR
This is an exported `FunctionDeclaration` named `onGetNotifications`, but it does not yet have a dedicated description in the generated reference.
