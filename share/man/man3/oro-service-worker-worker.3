.TH ORO-SERVICE-WORKER-WORKER 3 "" "Oro Runtime" "Oro Runtime API Manual"
.SH NAME
oro:service\-worker/worker \- JavaScript API module
.SH SYNOPSIS
.nf
import * as module from 'oro:service\-worker/worker'
.fi
.SH DESCRIPTION
.PP
oro:service\-worker/worker is a JavaScript API module exposed by the Oro Runtime.
.SH REFERENCE
.TP
\fBSERVICE_WORKER_READY_TOKEN\fR
This is an exported `VariableDeclaration` named `SERVICE_WORKER_READY_TOKEN`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBmodule\fR
This is an exported `VariableDeclaration` named `module`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBevents\fR
This is an exported `VariableDeclaration` named `events`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBstages\fR
This is an exported `VariableDeclaration` named `stages`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonReady()\fR
This is an exported `FunctionDeclaration` named `onReady`, but it does not yet have a dedicated description in the generated reference.
.TP
\fBonMessage(event)\fR
This is an exported `FunctionDeclaration` named `onMessage`, but it does not yet have a dedicated description in the generated reference.
