/**
 * @notice This is a rexports of `fs/index.js` so consumers will
 * need to only `import * as fs from 'oro:fs'`
 */
import * as exports from './fs/index.js'
export * from './fs/index.js'
export default exports
