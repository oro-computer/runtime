# TLS test fixtures

The certificates and private keys in this directory are public test fixtures. They provide stable
inputs for automated tests and are not secrets. Never use these keys or trust this certificate
authority outside the Oro Runtime test suite.
